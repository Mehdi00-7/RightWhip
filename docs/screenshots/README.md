Add the four screenshots referenced by the README here:

- browse.png       — /listings with the filter sidebar
- listing.png      — /listings/<id> showing the price badge + seller contact + map
- nl-search.png    — the homepage NL search box with a query typed
- map.png          — /map with clusters and the radius control
